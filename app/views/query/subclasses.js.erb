$("#qb_properties_sub_classes_loading").hide("fast");
$("#property_main_subclass_header").find("button").first().hide("fast");
		
	var list_group = $("#property_main_subclasses_group").find(".list-group-item").first();
<%@subclasses.each do |sc|%>
	var html = "<a href='javascript:void(0);' class='property-subclass-individual list-group-item' uri='<%=sc[:uri]%>'><%=sc[:name].capitalize%></a>";
	$(list_group).after(html);
<%end%>

